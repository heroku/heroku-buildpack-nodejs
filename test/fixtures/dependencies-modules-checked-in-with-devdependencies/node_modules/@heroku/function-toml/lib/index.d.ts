import * as Ajv from 'ajv';
export declare class FunctionToml {
    validator: Promise<Ajv.ValidateFunction>;
    constructor(schemaPath?: string);
    parseFile(filepath: string): Promise<any>;
    parseData(data: any): Promise<any>;
    private readSchema;
    private createValidator;
}
