language: bash
sudo: required
services:
  - docker
env:
  - TEST=heroku-18 STACK=heroku-18
  - TEST=heroku-16 STACK=heroku-16
  - TEST=cedar-14 STACK=cedar-14
  - TEST=hatchet STACK=heroku-18
install: docker pull "heroku/${STACK/-/:}"
script: make test-${TEST}
