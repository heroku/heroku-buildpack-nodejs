#!/usr/bin/env bash
# bin/release <build-dir>

if $YARN; then
  cat << EOF
addons: []
default_process_types:
  web: yarn start
EOF
else
  cat << EOF
addons: []
default_process_types:
  web: npm start
EOF
fi
