#!/usr/bin/env bash
# bin/detect <build-dir>

if [ -d $1/.meteor ]; then
  echo "Node.js (Meteor)" && exit 0
elif [ -f $1/package.json ]; then
  echo "Node.js" && exit 0
else
  echo "no" && exit 1
fi
