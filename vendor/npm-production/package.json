{
  "name": "heroku-buildpack-npm-productionify",
  "version": "0.0.1",
  "devDependencies": {
    "coffee-script": "*"
  },
  "scripts": {
    "pretest": "echo '#!/usr/bin/env node' > npm && ./node_modules/.bin/coffee -p npm.coffee >> npm && chmod a+x npm",
    "test": "/usr/bin/true"
  }
}
